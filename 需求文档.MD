# 桌面贪吃蛇进度条 — 项目需求文档（PRD）

## 1. 项目概述

### 1.1 基本信息

| 项目 | 内容 |
|:---|:---|
| **项目名称** | 桌面贪吃蛇进度条（SnakeProgress） |
| **目标平台** | Windows 10+ / macOS 12+ / Linux (X11/Wayland) |
| **软件类型** | 桌面常驻工具 / 系统托盘应用 |
| **核心功能** | 在屏幕四周边框以"贪吃蛇"像素风格动画，可视化展示当日工作进度 |
| **目标用户** | 需要时间管理、工作节奏感知的上班族、自由职业者、远程工作者 |

### 1.2 背景与动机

上班族在长时间工作中容易陷入"时间盲区"——不清楚一天已过去多少、还剩多少，导致过度加班或节奏失控。现有时间管理工具多为独立窗口/弹窗型，需要主动查看，无法做到**无感、持续、直觉**的进度感知。

### 1.3 核心价值主张

> **把"时间进度"变成"屏幕边框的一部分"**——无需打开任何应用，只需瞟一眼屏幕边框，就知道工作日走了多远。

- **零交互成本**：常驻屏幕边框，不遮挡工作内容，鼠标穿透不干扰操作
- **直觉感知**：像素蛇的长度/位置直观映射进度，无需阅读数字
- **趣味性**：经典贪吃蛇元素带来轻松感，缓解工作焦虑
- **轻量极简**：极低资源占用，开机自启无负担

### 1.4 产品定位

| 维度 | 定位 |
|:---|:---|
| **使用场景** | 工作日全时段常驻，非工作日/非工作时段自动隐藏或切换状态 |
| **交互深度** | 设置一次后基本无需操作，属于"Set and Forget"型工具 |
| **竞品差异化** | 相比进度条/时钟类工具，贪吃蛇像素风格是唯一的趣味化+可视化结合方案 |

---

## 2. 用户场景

### 场景 1：日常上班进度感知

> **角色**：小李，互联网公司产品经理  
> **场景**：早上 9:00 到岗，开启电脑，SnakeProgress 自动启动。蛇从左上角出发，缓慢沿边框行进。午休时蛇身暂停并变色提示。下午 6:00 蛇到达终点，触发庆祝动画。  
> **期望**：无需任何操作，一整天都能直觉感知进度

### 场景 2：全屏演示时自动隐藏

> **角色**：王姐，销售总监  
> **场景**：下午给客户做 PPT 演示，进入全屏模式。SnakeProgress 检测到全屏应用后自动隐藏，演示结束后自动恢复。  
> **期望**：不影响正式场合，无需手动关闭

### 场景 3：自定义个性化主题

> **角色**：小张，前端开发工程师  
> **场景**：不喜欢默认绿色，在设置中切换为"赛博朋克"主题（霓虹渐变蛇身 + 深色蛇头），调大像素到 12px，开启彩虹模式。  
> **期望**：风格可高度自定义，彰显个性

### 场景 4：多显示器办公

> **角色**：赵工，数据分析师  
> **场景**：使用双屏，主屏写代码、副屏看数据。选择只在主屏显示进度蛇，副屏保持干净。  
> **期望**：灵活选择显示位置

---

## 3. 功能需求

### 3.1 核心进度展示

| 功能点 | 描述 | 优先级 | 验收标准 |
|:---|:---|:---:|:---|
| **时间进度计算** | 根据用户设定的上下班时间，实时计算当日工作进度百分比 | P0 | 进度精度 ±1%；跨午休时段进度计算正确（扣除午休时长） |
| **贪吃蛇动画** | 蛇身沿屏幕边框移动，头部代表当前进度位置，蛇身长度代表已完成进度 | P0 | 蛇头位置与进度百分比精确对应；动画帧率 ≥ 30fps |
| **四边环绕** | 蛇从屏幕左上角出发，按顺时针方向沿上→右→下→左边框移动 | P0 | 蛇在四角平滑转弯，无断裂/跳帧 |
| **像素风格** | 蛇身由方块像素组成，支持自定义像素大小 | P1 | 像素方块排列整齐无间隙，无亚像素渲染模糊 |
| **头部特效** | 蛇头带闪烁/发光效果，颜色与蛇身可区分 | P1 | 蛇头视觉辨识度明显高于蛇身，不影响整体性能 |
| **完成庆祝** | 到达下班时间时，触发庆祝动画（蛇身闪烁 + 彩带粒子） | P2 | 庆祝动画持续 3~5 秒后自动停止；可关闭此功能 |
| **非工作时段状态** | 上班前显示"等待上班"状态（蛇静止在起点）；下班后显示"已完成"状态（蛇静止在终点） | P0 | 状态切换平滑，无闪烁；非工作日显示独立状态标识 |
| **进度百分比显示** | 鼠标悬停蛇身时，显示当前进度百分比 Tooltip | P1 | 悬停后 300ms 内出现提示，移开后 200ms 内消失 |

> **边界条件说明**：
> - 当进度 = 0% 时，仅显示蛇头在起点位置
> - 当进度 = 100% 时，蛇身完整填满四边（或到达终点，取决于蛇身长度模式）
> - 跨午夜时段（如夜班 22:00-06:00）需正确处理日期跨越
> - 系统休眠/唤醒后，进度应自动同步到当前时间，不沿用休眠前的位置

### 3.2 时间设置

| 功能点 | 描述 | 优先级 | 验收标准 |
|:---|:---|:---:|:---|
| **上下班时间** | 支持设置上班时间和下班时间（精确到分钟） | P0 | 时间选择器支持 00:00~23:59；下班时间必须晚于上班时间（跨午夜除外） |
| **午休时间** | 可选设置午休时段，进度条在午休期间暂停 | P1 | 午休期间蛇身暂停移动并变色（如变灰/变暗）；午休结束后自动恢复 |
| **多时段支持** | 支持设置多个工作时段（如上午班 + 下午班） | P2 | 最多支持 3 个时段；时段不可重叠；各时段独立计算进度 |
| **工作日设置** | 可选择生效星期（周一至周日），非工作日自动隐藏或显示不同状态 | P1 | 默认周一至周五；非工作日显示灰色/半透明蛇或完全隐藏 |
| **倒计时模式** | 悬浮提示中切换显示"距离下班还有 X 小时 X 分" | P2 | 倒计时每分钟更新；午休时段倒计时也暂停 |

> **校验规则**：
> - 上班时间与下班时间不可相同
> - 午休时段必须在上班时间范围内
> - 多时段场景下，时段之间至少间隔 10 分钟
> - 时间格式统一为 24 小时制（HH:mm）

### 3.3 样式自定义

| 功能点 | 描述 | 优先级 | 验收标准 |
|:---|:---|:---:|:---|
| **蛇身颜色** | 支持纯色 / 线性渐变 / 彩虹色三种模式 | P0 | 渐变色沿蛇身方向过渡自然；彩虹色均匀覆盖全色相环 |
| **蛇头颜色** | 蛇头可独立设置颜色，默认与蛇身末段同色 | P1 | 蛇头颜色在视觉上与蛇身可区分（相同颜色时自动加深 20%） |
| **背景透明度** | 蛇身整体透明度调节 | P0 | 0% 完全透明（不可见）~ 100% 完全不透明；步进 5% |
| **像素大小** | 调节贪吃蛇方块像素尺寸 | P1 | 范围 2px~20px，步进 2px；像素变大时蛇身自动减少方块数量 |
| **边框边距** | 蛇身距离屏幕边缘的间距 | P1 | 0px（紧贴边缘）~ 20px，步进 1px |
| **蛇身长度模式** | ① 尾随模式：蛇头到起点为已完成进度；② 固定长度：固定长度蛇跟随进度移动 | P2 | 尾随模式为默认；固定长度模式下蛇长默认为总周长的 20%，可调节 10%~50% |
| **主题预设** | 内置多套主题（经典绿 / 赛博朋克 / 像素红白机 / 暗黑 / 海洋 / 樱花） | P1 | 切换主题后所有样式参数联动更新；用户可在预设基础上二次修改 |
| **动画速度** | 蛇身蠕动动画的速度调节 | P2 | 支持 慢速/正常/快速 三档；正常档位蛇身每秒移动 1 个像素位 |

### 3.4 显示控制

| 功能点 | 描述 | 优先级 | 验收标准 |
|:---|:---|:---:|:---|
| **显示/隐藏** | 全局快捷键或托盘菜单快速切换 | P0 | 默认快捷键 `Ctrl+Shift+S`；隐藏后蛇身淡出（200ms），显示时淡入（200ms） |
| **多显示器支持** | 可选择在哪块显示器上显示 | P1 | 下拉列表枚举所有显示器名称；默认主显示器；显示器断开后自动切换到主屏 |
| **置顶 + 鼠标穿透** | 窗口始终置顶，但不拦截鼠标点击 | P0 | 蛇身区域鼠标点击穿透到下层窗口；Windows 使用 `WS_EX_TRANSPARENT`，macOS 使用 `NSWindow.ignoresMouseEvents` |
| **全屏检测** | 检测到全屏应用时自动隐藏 | P1 | 检测延迟 < 500ms；退出全屏后恢复延迟 < 1s；支持白名单（全屏时也显示） |
| **特定应用隐藏** | 设置黑名单应用，前台时自动隐藏 | P2 | 按进程名匹配；支持通配符（如 `*.exe`）；最多 20 条规则 |
| **开机自启** | 支持设置开机自动启动 | P1 | 默认开启；通过系统注册表/LaunchAgent/autostart 实现 |

### 3.5 系统托盘

| 功能点 | 描述 | 优先级 | 验收标准 |
|:---|:---|:---:|:---|
| **托盘图标** | 显示当前进度的迷你图标 | P1 | 图标动态反映进度（如迷你蛇形或百分比数字）；进度变化时图标更新 |
| **右键菜单** | 设置 / 显示隐藏 / 开机自启 / 退出 / 关于 | P0 | 菜单项完整可用；显示/隐藏为勾选切换；退出需二次确认 |
| **悬浮提示** | 鼠标悬停托盘图标显示进度和剩余时间 | P1 | 提示格式："工作进度 62% · 剩余 3h 12min"；每分钟更新 |

### 3.6 数据持久化

| 功能点 | 描述 | 优先级 | 验收标准 |
|:---|:---|:---:|:---|
| **配置自动保存** | 任何设置变更后自动保存，无需手动点击 | P0 | 变更后 500ms 内写入磁盘；配置文件损坏时自动回退到默认值 |
| **配置导入导出** | 支持 JSON 格式的配置备份与恢复 | P2 | 导出包含完整配置 + 版本号；导入时校验版本兼容性，不兼容时提示用户 |
| **配置迁移** | 版本升级时自动迁移旧版配置 | P1 | 新增字段使用默认值填充；废弃字段自动清理；迁移失败时保留旧配置并提示 |

---

## 4. 非功能需求

| 类别 | 要求 | 验收方法 |
|:---|:---|:---|
| **CPU 占用** | 空闲态 < 0.5%，动画态 < 1%（单核） | 任务管理器/Activity Monitor 持续监测 5 分钟取均值 |
| **内存占用** | 常驻内存 < 50MB（不含渲染缓冲区） | 进程内存监控，运行 1 小时后测量，无内存泄漏（增长 < 1MB/小时） |
| **动画帧率** | 稳定 ≥ 30fps，无卡顿掉帧 | 帧率计数器连续监测，99% 帧间隔 < 33ms |
| **兼容性** | Windows 10 21H2+ / macOS 12+ / Ubuntu 22.04+ (X11 & Wayland) | 各平台手动验证核心功能 |
| **稳定性** | 7×24 小时连续运行无崩溃；分辨率变化/显示器热插拔后 3s 内自适应 | 自动化压测脚本 + 手动触发事件验证 |
| **权限** | 无需管理员/root 权限运行 | 普通用户账户安装并运行全功能 |
| **隐私** | 不上传任何用户数据；无网络请求（除自动更新检查外） | 网络抓包验证；源码审计 |
| **安装包体积** | < 30MB（Windows 安装包） | 构建产物测量 |
| **冷启动速度** | 从双击图标到蛇身出现 < 2 秒 | 计时测量，取 10 次中位数 |
| **热启动速度** | 从系统唤醒到蛇身恢复 < 1 秒 | 休眠/唤醒后计时 |
| **卸载干净度** | 卸载后不残留配置文件、注册表项、后台进程 | 卸载后全盘扫描验证 |

---

## 5. 界面设计

### 5.1 设置面板（Settings Window）

```
┌─────────────────────────────────────────┐
│  ⚙️ 贪吃蛇进度条设置          [—][□][×]  │
├─────────────────────────────────────────┤
│                                         │
│  📅 工作时间                             │
│  ┌─────────────────────────────────────┐│
│  │ 上班时间  [ 09:00 ]  下班时间 [18:00]││
│  │ 午休时段  [ 12:00 ] ~ [ 13:00 ]  ☑ ││
│  │ 工作日    ☑一 ☑二 ☑三 ☑四 ☑五 ☐六 ☐日││
│  └─────────────────────────────────────┘│
│                                         │
│  🎨 外观样式                             │
│  ┌─────────────────────────────────────┐│
│  │ 主题预设  [ 经典绿 ▼ ]              ││
│  │ 蛇身颜色  [🟩]  蛇头颜色 [🟨]       ││
│  │ 彩虹模式  [ 关闭 ▼ ]                ││
│  │ 像素大小  [====●====] 8px           ││
│  │ 透明度    [====●====] 80%           ││
│  │ 边框边距  [====●====] 2px           ││
│  │ 长度模式  [ 尾随模式 ▼ ]            ││
│  │ 动画速度  [ 正常 ▼ ]                ││
│  └─────────────────────────────────────┘│
│                                         │
│  🖥️ 显示设置                             │
│  ┌─────────────────────────────────────┐│
│  │ 显示显示器 [ 主显示器 ▼ ]            ││
│  │ 全屏自动隐藏  ☑                     ││
│  │ 鼠标穿透      ☑                     ││
│  │ 开机自启      ☑                     ││
│  └─────────────────────────────────────┘│
│                                         │
│  [ 恢复默认 ]            [ 保存并关闭 ]  │
└─────────────────────────────────────────┘
```

**交互说明**：
- 设置面板为独立窗口，非模态，可同时看到蛇身效果
- 所有滑块/颜色选择器修改后**实时预览**，无需点击保存
- "恢复默认"需二次确认弹窗
- 首次启动自动弹出设置面板引导用户配置工作时间

### 5.2 屏幕效果示意

```
┌─────────────────────────────────────────────────────────────┐
│ ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │  ← 上边框
│ █                                                            │
│ ░                                                            │
│ ░                                                            │
│ ░                                                            │
│ ░                                                           █ │  ← 右边框
│ ░                                                           ░ │
│ ░                                                           ░ │
│ ░                                                           ░ │
│ ░                                                           ░ │
│ ░                                                           ░ │
│ ░                                                           ░ │
│ ░                                                           ░ │
│ █                                                           ░ │
│ ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │  ← 下边框
│  ↑蛇头   ↑蛇身（已完成进度，尾随模式）                          │
└─────────────────────────────────────────────────────────────┘

图例：
  █ = 蛇身（已完成进度部分）
  ░ = 蛇身路径（未完成部分，可选显示为淡色轨迹或不显示）
  蛇头 = 当前进度位置，带发光/闪烁特效
```

**视觉效果补充说明**：
- 蛇头：方块略大于蛇身（如蛇身 8px，蛇头 10px），带 1px 发光描边
- 蛇身：每个像素方块之间留 1px 间隙（可选），增加像素感
- 蛇身轨迹：未完成部分可选择性显示半透明虚线/暗色轨迹，帮助感知总长度
- 角落转弯：蛇身在角落平滑转弯，角落方块使用 45° 旋转方块或圆角过渡

---

## 6. 技术选型

### 6.1 方案对比与决策

| 维度 | Tauri ⭐ 推荐 | Electron | Flutter |
|:---|:---|:---|:---|
| **安装包体积** | ~5MB | ~80MB | ~20MB |
| **内存占用** | ~15MB | ~150MB | ~50MB |
| **启动速度** | < 1s | ~3s | ~2s |
| **跨平台窗口穿透** | Rust 调用原生 API，灵活 | 成熟方案 | 需要插件 |
| **透明无边框窗口** | 原生支持 | 原生支持 | 需要插件 |
| **渲染方式** | WebView (HTML/CSS/Canvas) | Chromium | Skia |
| **开发效率** | 前端技术栈 + Rust | 纯前端技术栈 | Dart |
| **社区生态** | 快速增长，较新 | 非常成熟 | 成熟 |

### 6.2 推荐方案：Tauri

**决策理由**：
1. **体积与性能**：本应用的核心诉求是"轻量常驻"，Tauri 的 ~5MB 体积和 ~15MB 内存远优于 Electron
2. **窗口穿透**：Tauri 的 Rust 层可直接调用系统原生 API，实现 `WS_EX_TRANSPARENT`/`ignoresMouseEvents` 等穿透效果
3. **渲染够用**：贪吃蛇动画仅涉及 2D 像素方块移动，WebView 的 Canvas 2D 完全胜任
4. **安全隐私**：Tauri 默认无远程调用能力，符合"纯本地、无网络请求"的隐私要求

### 6.3 技术架构

```
┌──────────────────────────────────────────────┐
│                   Tauri App                   │
├──────────────────┬───────────────────────────┤
│   Frontend       │   Backend (Rust)          │
│   (HTML/CSS/JS)  │                           │
│                  │   ┌─────────────────────┐ │
│  ┌────────────┐  │   │ Window Manager      │ │
│  │ Canvas 2D  │◄─┼───│ - 透明无边框窗口     │ │
│  │ Animation  │  │   │ - 鼠标穿透           │ │
│  │ Settings UI│  │   │ - 多显示器管理       │ │
│  └────────────┘  │   ├─────────────────────┤ │
│                  │   │ System Integration   │ │
│                  │   │ - 系统托盘           │ │
│                  │   │ - 开机自启           │ │
│                  │   │ - 全屏检测           │ │
│                  │   │ - 快捷键注册         │ │
│                  │   ├─────────────────────┤ │
│                  │   │ Config Manager       │ │
│                  │   │ - JSON 读写          │ │
│                  │   │ - 配置迁移           │ │
│                  │   │ - 导入导出           │ │
│                  │   └─────────────────────┘ │
└──────────────────┴───────────────────────────┘
```

### 6.4 关键技术要点

| 技术点 | 实现方案 |
|:---|:---|
| **透明无边框窗口** | Tauri `decorations: false` + `transparent: true` |
| **鼠标穿透** | Windows: `WS_EX_LAYERED \| WS_EX_TRANSPARENT`；macOS: `ignoresMouseEvents`；Linux: X11 `_NET_WM_WINDOW_TYPE_DOCK` |
| **始终置顶** | Tauri `always_on_top: true` + `window_level: "floating"` |
| **全屏检测** | Windows: `EnumWindows` 检测全屏窗口；macOS: `NSWorkspace` 全屏通知；Linux: EWMH `_NET_WM_STATE_FULLSCREEN` |
| **蛇身渲染** | HTML Canvas 2D，`requestAnimationFrame` 驱动，帧率限制 30fps |
| **配置存储** | JSON 文件，存放在 `~/.snakeprogress/config.json`（跨平台路径由 Tauri `app_data_dir` 提供） |
| **开机自启** | Windows: 注册表 `Run` 键；macOS: `LaunchAgent` plist；Linux: `.config/autostart/` .desktop 文件 |

---

## 7. 数据结构

### 7.1 配置文件 Schema

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "version": "1.0.0",
  "workTime": {
    "start": "09:00",
    "end": "18:00",
    "lunch": {
      "enabled": true,
      "start": "12:00",
      "end": "13:00"
    },
    "periods": [],
    "workdays": [1, 2, 3, 4, 5]
  },
  "appearance": {
    "theme": "classic",
    "snakeColor": "#00FF00",
    "headColor": "#FFFF00",
    "colorMode": "solid",
    "rainbowMode": false,
    "pixelSize": 8,
    "opacity": 80,
    "margin": 2,
    "snakeLengthMode": "trailing",
    "fixedLengthPercent": 20,
    "animationSpeed": "normal",
    "showTrail": false,
    "headGlow": true
  },
  "display": {
    "monitor": "primary",
    "autoHideFullscreen": true,
    "fullscreenWhitelist": [],
    "clickThrough": true,
    "hideOnApps": [],
    "autoStart": true,
    "showOnNonWorkdays": false,
    "nonWorkdayStyle": "hidden"
  },
  "shortcut": {
    "toggleVisibility": "Ctrl+Shift+S"
  },
  "celebration": {
    "enabled": true,
    "duration": 3000,
    "type": "fireworks"
  }
}
```

### 7.2 字段校验规则

| 字段 | 类型 | 范围/格式 | 默认值 |
|:---|:---|:---|:---|
| `workTime.start` | string | `HH:mm`，24h 制 | `"09:00"` |
| `workTime.end` | string | `HH:mm`，必须晚于 start（跨午夜除外） | `"18:00"` |
| `workTime.lunch.start/end` | string | `HH:mm`，必须在 start~end 范围内 | `"12:00"` / `"13:00"` |
| `workTime.workdays` | number[] | 1~7 的子集，不可为空 | `[1,2,3,4,5]` |
| `appearance.pixelSize` | number | 2~20，步进 2 | `8` |
| `appearance.opacity` | number | 0~100，步进 5 | `80` |
| `appearance.margin` | number | 0~20，步进 1 | `2` |
| `appearance.snakeLengthMode` | enum | `"trailing"` \| `"fixed"` | `"trailing"` |
| `appearance.animationSpeed` | enum | `"slow"` \| `"normal"` \| `"fast"` | `"normal"` |
| `display.hideOnApps` | string[] | 最多 20 条，支持 `*` 通配符 | `[]` |
| `shortcut.toggleVisibility` | string | 合法快捷键格式 | `"Ctrl+Shift+S"` |

### 7.3 主题预设定义

| 主题名 | snakeColor | headColor | colorMode | pixelSize | opacity | 说明 |
|:---|:---|:---|:---|:---|:---|:---|
| `classic` | `#00FF00` | `#FFFF00` | solid | 8 | 80 | 经典绿，默认主题 |
| `cyberpunk` | `#FF00FF→#00FFFF` | `#FFFFFF` | gradient | 8 | 90 | 赛博朋克，霓虹渐变 |
| `retro` | `#FF0000` | `#FFFFFF` | solid | 4 | 100 | 像素红白机，小像素 |
| `dark` | `#555555` | `#AAAAAA` | solid | 8 | 60 | 暗黑模式，低调 |
| `ocean` | `#0077BE→#00CED1` | `#FFFFFF` | gradient | 10 | 85 | 海洋渐变 |
| `sakura` | `#FFB7C5→#FF69B4` | `#FFFFFF` | gradient | 8 | 75 | 樱花粉渐变 |

---

## 8. 里程碑规划

### MVP（最小可行产品）— 2 周

**目标**：核心功能可用，能在屏幕上看到蛇在跑

| 任务 | 预估 | 交付物 |
|:---|:---|:---|
| 项目脚手架搭建（Tauri + 前端） | 0.5 天 | 可运行的空白窗口 |
| 透明无边框窗口 + 鼠标穿透 + 置顶 | 1 天 | 屏幕上出现透明悬浮窗口 |
| Canvas 蛇身渲染（静态进度） | 1 天 | 根据进度百分比绘制蛇身 |
| 时间进度计算引擎 | 0.5 天 | 精确的进度百分比计算 |
| 蛇身动画（蠕动效果） | 1 天 | 蛇身有蠕动动画，随时间推移移动 |
| 基础设置面板（工作时间 + 颜色 + 透明度） | 2 天 | 可配置的设置界面 |
| 配置自动保存/加载 | 0.5 天 | 设置持久化 |
| 系统托盘（图标 + 右键菜单） | 1 天 | 托盘基本功能 |
| Windows 平台测试 & Bug 修复 | 0.5 天 | 稳定运行 |

**验收标准**：在 Windows 上启动后，能根据设定的工作时间显示贪吃蛇进度，支持基本配置

### V1.0 — +2 周

**目标**：功能完善，可日常使用

| 任务 | 预估 | 交付物 |
|:---|:---|:---|
| 午休时段支持 | 1 天 | 午休期间蛇身暂停并变色 |
| 主题预设系统（6 套主题） | 1.5 天 | 可一键切换主题 |
| 全屏检测 + 自动隐藏 | 1 天 | 全屏应用时蛇自动隐藏 |
| 快捷键注册 | 0.5 天 | 全局快捷键切换显示 |
| 非工作日/非工作时段状态 | 0.5 天 | 合理的状态展示 |
| 开机自启 | 0.5 天 | 系统启动时自动运行 |
| macOS 适配 | 2 天 | macOS 上功能完整 |
| 托盘图标动态进度 + 悬浮提示 | 0.5 天 | 托盘信息完善 |
| 测试 & Bug 修复 | 1.5 天 | 稳定运行 |

**验收标准**：Windows + macOS 双平台核心功能完整，可日常使用

### V1.5 — +2 周

**目标**：增强体验，打磨细节

| 任务 | 预估 | 交付物 |
|:---|:---|:---|
| 多显示器支持 | 1.5 天 | 可选择显示屏幕 |
| 多时段支持 | 1 天 | 支持多个工作时段 |
| 完成庆祝动画 | 1 天 | 下班时的庆祝效果 |
| 配置导入导出 | 0.5 天 | JSON 配置备份恢复 |
| Linux 适配 | 2 天 | Linux 上功能完整 |
| 特定应用黑名单 | 0.5 天 | 指定应用前台时隐藏 |
| 蛇身长度模式（固定长度） | 0.5 天 | 两种长度模式 |
| 倒计时显示 | 0.5 天 | 悬浮提示显示倒计时 |
| 多语言支持（中/英） | 0.5 天 | 界面语言切换 |
| 测试 & Bug 修复 | 1 天 | 全平台稳定运行 |

**验收标准**：三平台功能完整，所有 P1 需求已实现

---

## 9. 风险与对策

| 风险 | 概率 | 影响 | 对策 | 预留方案 |
|:---|:---:|:---:|:---|:---|
| 多平台窗口穿透 API 差异 | 高 | 高 | 优先保证 Windows；各平台单独封装穿透模块 | 降级为普通置顶窗口（不穿透） |
| 高分辨率/多 DPI 适配 | 高 | 中 | 使用物理像素计算蛇身位置；监听 DPI 变化事件动态重算 | 强制 1x 渲染，不做 DPI 缩放 |
| Wayland 下窗口穿透受限 | 中 | 高 | 优先支持 X11；Wayland 下使用 Layer Shell 协议 | Wayland 下降级为普通窗口 |
| 与杀毒软件误报 | 中 | 低 | 代码签名；开源透明；提交各杀软白名单 | 提供手动加白名单教程 |
| 全屏检测 API 兼容性 | 中 | 中 | 各平台分别实现；提供全屏白名单作为 fallback | 手动快捷键隐藏/显示 |
| 休眠/唤醒后时间不同步 | 低 | 中 | 监听系统唤醒事件，重新计算进度 | 定时轮询（每 10s）作为兜底 |
| Tauri 生态不成熟导致 API 缺失 | 低 | 高 | 核心功能提前做 PoC 验证；预留 Rust 自定义命令扩展点 | 降级到 Electron 方案 |

---

## 10. 开放问题与待决事项

| # | 问题 | 状态 | 备注 |
|:---|:---|:---:|:---|
| 1 | 蛇身是否需要"食物"机制（蛇吃到食物后变长）？ | 待定 | 可增加趣味性，但与进度语义冲突，需权衡 |
| 2 | 是否支持多条蛇（如一周七天每天一条）？ | 待定 | 增加视觉信息量，但可能造成干扰 |
| 3 | 是否支持"番茄钟"模式（25分钟工作+5分钟休息循环）？ | 待定 | 与核心"整日进度"定位不同，可作为独立模式 |
| 4 | 蛇身动画是否需要音效？ | 待定 | 大多数场景不适合音效，可能仅庆祝动画可选 |
| 5 | 是否需要自动更新功能？ | 待定 | 增加开发成本，但有助于版本迭代 |
| 6 | Wayland 下鼠标穿透是否可行？ | 调研中 | Wayland 安全模型限制较多，需做技术调研 |
| 7 | 是否上架各平台应用商店（Microsoft Store / Mac App Store）？ | 待定 | 需要开发者账号费用和审核流程 |

---

> **文档版本**: v2.0  
> **创建日期**: 2026-08-04  
> **最后更新**: 2026-08-04  
> **状态**: 评审中  
> **作者**: AI Assistant  
> **变更记录**: v1.0 → v2.0 全面优化，新增用户场景、验收标准、技术架构、开放问题等章节
